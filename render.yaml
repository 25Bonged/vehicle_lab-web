services:
  - type: web
    name: vehicle-lab
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /
    # PORT is automatically set by Render - app.py will detect it

